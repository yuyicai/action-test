name: pull_request_target

on:
  pull_request_target:
    branches: [ main ]

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v3

      - name: echo test
        run: |
          echo "test"
